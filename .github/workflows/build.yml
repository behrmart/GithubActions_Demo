# This workflow will build a JavaScript Example
on: push
jobs:
    first-job:
        # runs-on: windows-latest
        runs-on: ubuntu-latest
        steps:
            - run: node --version
            - run: npm --version 

